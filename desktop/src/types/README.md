# types/

Shared TypeScript types and Zod schemas that mirror the API contracts in
`docs/api.md` and the SQLite schema in `docs/database.md`. Feature-local
types live inside their own `features/<name>/` module.
