# api/

Typed client for the local HTTP API exposed by the Tauri/Rust backend
(`src-tauri/`). Populated in Phase 10 (Local API) once the API server and
its contracts exist; consumed by TanStack Query hooks in `features/`.
