# features/

Feature-first modules (`auth/`, `products/`, `pos/`, `inventory/`,
`customers/`, `cash-register/`, `reports/`, `devices/`, `settings/`, ...).
Each module owns its components, hooks, and TanStack Query
queries/mutations. Populated starting Phase 3 (Authentication + RBAC).
