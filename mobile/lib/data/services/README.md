# data/services/

Thin wrappers around the Dio client for each API resource (auth, pairing,
products, sales, sync, ...). Repositories in `data/repositories/` compose
these with the local offline queue.
