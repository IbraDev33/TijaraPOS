# core/network/

Dio client setup (base URL from the paired server, auth interceptor that
attaches the device token, retry/idempotency-key interceptor) and the
connectivity/discovery helpers backing the connection indicator. Populated
in Phase 11 (Mobile pairing/authentication).
